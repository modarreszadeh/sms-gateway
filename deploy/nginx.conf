user nginx;

worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 4000;

        location / {
            proxy_pass http://127.0.0.1:5000;
        }
    }

}